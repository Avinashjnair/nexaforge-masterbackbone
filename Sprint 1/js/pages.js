/* ============================================================
   NexaForge ERP — Page stubs (modules to be built out)
   ============================================================ */

// All non-dashboard/non-project pages currently fall through
// to renderPlaceholderPage() in app.js.
// As each module gets built, its renderer replaces the placeholder.
// This file is the extension point for those renderers.

// Reserved namespace for upcoming module renderers:
// renderMarketing(), renderProduction(), renderQuality(),
// renderProcurement(), renderInventory(), renderFinance(),
// renderHR(), renderWelding(), renderAnalytics()
